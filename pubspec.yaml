name: mds_home
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  csv: ^5.0.0  # Add CSV support
  cupertino_icons: ^1.0.2
  http: ^1.2.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2 # For saving theme preference
  js: ^0.6.7
  path_provider: ^2.0.15
  share_plus: ^6.0.0
  url_launcher: ^6.1.10
  universal_html: ^2.2.2
  image: ^4.0.15  # for image processing
  firebase_core: ^2.24.2
  firebase_analytics: ^10.7.4
  cloud_firestore: ^4.13.6
  image_gallery_saver: ^2.0.3
  cloud_functions: ^4.3.2  # Add this line
  cached_network_image: ^3.3.0
  flutter_markdown: ^0.6.18
  flutter_web_plugins:
    sdk: flutter
  intl: ^0.19.0
  fl_chart: ^0.71.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/2024/available_players.csv
    - assets/2024/draft_order.csv
    - assets/2024/team_needs.csv
    - assets/2025/available_players.csv
    - assets/2025/draft_order.csv
    - assets/2025/team_needs.csv
    - assets/2025/player_descriptions.csv
    - assets/2025/live_picks.csv
    - assets/draft_value_chart.csv
    - assets/blog_posts.json
    - assets/data/historical_nfl_matchups.csv

